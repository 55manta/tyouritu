# 調律ノート — プロジェクト引き継ぎ書

> このファイルは「別のPC・別のClaudeアカウントで続きを行う」ための引き継ぎ資料です。
> 新しいセッションで、このファイルの内容を Claude に読ませれば、経緯・決定事項・現状・次のアクションをすべて把握して続きから作業できます。
> 最終更新：2026-08-13（2026-08-12セッションのHANDOFF_調律ノート.mdパックを統合）

---

## 0. 使い方（新しいPC・アカウントでの再開手順）

1. 下記「9. 同梱ファイル」を、この引き継ぎ書ごと新しいPCにコピーする（クラウドドライブ/USB/GitHub/自分宛メール等）
2. 新しいPCで Claude Code（または claude.ai）を開き、新規セッションを開始
3. この `HANDOFF.md` と `choritsu-note.html` を渡し、こう伝える：
   「調律ノートというアプリを作っている。この引き継ぎ書と現状のモック（choritsu-note.html）を読んで、続きから作業したい」
4. 以降は通常どおり指示すればOK

---

## 1. プロジェクト概要

- **何を作っているか**：ピアノ調律師（個人・フリーランス）向けのスマホアプリ「調律ノート」
- **目的**：オーナーの**個人の副業**。「細く長く月額課金」の収入源
- **中核価値**：**定期調律のリピート案内を自動化し失注を防ぐ**（双方が「相手からの連絡待ち」で年1回のリピートを取りこぼす、という裏取り済みの現場の痛みに対応）
- **現状**：単一HTMLファイルの動くプロトタイプが完成。**全18機能（S/A/B/C層）実装済み**、jsdomヘッドレステスト3スイート全通過。まだ本開発（クラウド化・認証・課金・ストア公開）は未着手

---

## 2. 経緯（テーマ選定・市場調査）

- サロン向け／トレーナー向け／養蜂家向けなど複数業種を比較検討した末、**ピアノ調律師向け**に決定（競合ほぼ不在・課題が明確・団体経由で営業可能）
- 国内唯一の競合「ピアノ元気？」（治郎吉商店、買い切り5〜20万円・PC専用・クラウド非対応）＝「高額・現場入力不可・通知機能なし」の隙間あり
- 米国 Gazelle（調律師専用SaaS）が同じ価値提案で成立、日本語版が無い
- **裏取り済みの痛み**：調律師「電話営業はしたくない」／顧客「連絡が来ないとそれきり」→ 自動リマインドで解決

### 市場調査の主要な事実（すべて2026-07-08〜08-10確認）
- 国内調律師は推定約1万人、うち独立フリーは3,000〜5,000人。JPTA会員は10年で3,000→2,000人と減少傾向（年3〜4%の自然減＝解約として現れる）
- 個人事業主のクラウド会計利用率は31%（＝7割は有料ツール未使用）
- 40〜60代はAndroidが約6割、70代は約7割（**iOS優先という当初の想定は誤り**）
- 未調律期間による割増料金（1年ごと1,000〜2,200円）は日本固有の商習慣
- 顧客の離脱は「不満」ではなく「断りにくさ」で起きる
- 調律師の技術作業内訳：調律25%／修理20%／整調10%／整音1%／その他44%（技術作業だけで見ると調律45%／修理・整調・整音55%）→ **単発案件を扱う根拠**
- 事務処理・確定申告の負担が調律師本人の一次情報で最も明確な不満
- 予定は「日時確定」「日付確定・時間未定」「時期到来・日付未定」の三層で実際に自作運用されている実例あり
- 米国Gazelleは「ピアノ個体単位の台帳」（顧客1:N台）を持つが日本語非対応。この構造が汎用ツールにない差別化点

---

## 3. 実装済み機能（全18機能、S/A/B/C層）

### S層（構造）
- 顧客とピアノを分離（1顧客:N台）。学校・音楽教室の複数台対応
- 予定の三層構造（日時確定／日付確定・時間未定／時期到来）
- 住所を番地まで＋駐車場・搬入経路の記録

### A層（売上直結）
- 前日確定アシスタント（同日複数件をエリア順に並べ移動時間込みで自動割付、一括連絡）
- 未調律期間の割増料金の自動計算（1年ごと+1,500円、上限15,000円、10年超は粗調律警告）※日本ロケール限定
- 写真つき作業報告書（現場で手ぶら入力できるよう軽量化、後述）
- ワンタップWeb予約（お客様側の画面。ログイン不要）
- 「今回は見送る」ボタン（お客様側。断りの電話をさせない設計）
- 繁閑の平準化（12ヶ月の混み具合グラフ＋前倒し提案）

### B層（事務負担軽減）
- 帳簿・確定申告CSV（freee/マネーフォワード/弥生形式、消費税自動計算）
- 追加作業のその場承諾（内容・金額・リスクを提示しお客様の承諾を記録）
- 法人のご請求（月締め・複数台まとめて1枚、インボイス対応）
- 前回との差分表示（ピッチ・湿度・状態の変化）
- ピアノ台帳の項目拡充（製造番号・製造年など）

### C層（低コスト・高効果）
- 訪問前3点セット（片付け・所要時間・駐車場の自動案内）
- 訪問者カード（顔写真・資格・車のナンバー）
- 事前問診（顧客が予約時に症状を選択）
- 道具の置き忘れチェックリスト

### 単発案件対応（重要な設計判断）
記録に**種別（kind）**を持たせ、**周期を動かすのは定期調律だけ**にした。以前は修理を記録すると無条件に次回時期が更新されるバグがあったが解消済み。
- 種別：定期調律／修理／整調・整音／点検／運搬・移動／その他
- お客様詳細に「臨時のご依頼を受ける」画面（種別・対象ピアノ・お困りごと・伺う日を入力）
- 修理の予定があっても「時期を迎える」一覧から消えない（定期調律の予定だけが該当ピアノを一覧から外す）
- お客様詳細に「この方との歩み」（お付き合い年数・訪問回数・累計売上・種別ごとの内訳）を追加
- 売上タブにも種別ごとの内訳を追加

### ワンソース国際化
- 住所を国際標準の構造に変更：`{country, postal, region, city, line1, line2}`。表示時にロケールで組み替え
- 設定画面に「国と地域」選択（日本／米国／英国／ドイツ）
- ロケールで変わるもの：住所欄の並び・呼び方、通貨表示（`Intl.NumberFormat`、日本のみ「13,000円」表記維持）、基準ピッチ初期値（日本442、欧米440）、税の呼び方（適格請求書番号／VAT number／Tax ID）
- **未調律期間の割増料金は日本固有と判断し、他ロケールでは機能ごと非表示**
- 金額表記50箇所を`money()`/`moneyHTML()`関数に統一

### 記録の修正・取り消し
- お客様詳細の履歴、売上一覧の両方から編集可能。日付を直すと次回のご案内時期も自動追随。取り消すとひとつ前の記録から再計算。請求済み・入金済みの記録には警告表示

---

## 4. UI/UXの主な変更点

- ホームを「今日やること」中心に再設計（訪問があれば最大表示、なければ従来のご案内ヒーロー）
- フォントサイズ70箇所以上を13.5px以上に底上げ（高齢層考慮）
- 料金内訳を折りたたみ式に
- お客様300軒でも破綻しないよう区分ごとに5軒で打ち切り「もっと見る」
- **初回起動の設計**（重要）：「次に伺ったお客様から1軒ずつ登録すれば1年で自然に揃う」というオンボーディング。サンプルデータの消去・復元も可能

### 作業報告書を「手ぶら」で書ける設計
現場は1台2〜3時間の肉体作業直後という前提：湿度・ピッチはチップ選択、状態は大きな二択、メモは10種の定型文タップ（使用頻度で並べ替え）、写真は`capture="environment"`でカメラ直起動、「前回と同じ内容にする」ボタン、入力ゼロでも送信可、後づけ追加導線。

---

## 5. デザイン仕様

- **コンセプト**：市場標準（ティール1色アクセント＋清潔な白）× ピアノらしさ（象牙・黒檀・真鍮）
- **色トークン**（ライト）：ground #F4F2EC / surface #FFFFFF / ink #1E1C17 / accent(teal) #0E8F83 / brass #B0873A / alert(コーラル) #C6503A
- **ダークモード**：`prefers-color-scheme` と `data-theme` トグル両対応
- **フォント**：OS標準（`-apple-system, system-ui, "Hiragino Sans", "Yu Gothic", Roboto...`）
- **アクセシビリティ**：本文16px以上、ボタン高48px以上、タップ操作のみ

### Android互換性（静的監査済み・実機未検証）
- 固定幅レイアウトなし、360px幅で横スクロールしない
- 入力欄は全て16px（iOSでの意図しない拡大防止）
- タップ領域44px未満だった箇所は修正済み
- `color-mix()`を22箇所使用（Chrome 111以降必須）→ 実装時はPostCSSでフォールバック色を自動生成すれば解決（コード変更不要）
- **未検証**：実機（特にAQUOS、360px幅）での目視確認

---

## 6. マネタイズ結論（最新版。旧monetize.mdの一部を上書き）

- **無料**：顧客**15軒**まで（旧案の30件から下方修正）。ただし**自動リマインドは無料でも完結**させる（唯一の「買う理由」を有料に隠さない、という前回案からの撤回済み）
- **有料**：**年額14,800円**（月額換算1,233円／月額単体プランは1,480円）。旧案の年19,800円から値下げ。「調律1件（約13,000円）ぶんの年会費」という説明が成立する価格設計
- 有料機能は「規模が大きくなると必要になるもの」に寄せる：前日確定アシスタント、割増料金の自動計算、会計CSV、法人請求、繁閑平準化、お客様の引き継ぎ
- 期間制トライアルは不採用（調律が年1周期のため30日では体験できない）
- **顧客台帳まるごとのエクスポート/インポート機能は実装しない**という明示の決定あり（会計CSV書き出しのみ）
- 詳細な市場・競合分析は `choritsu-note-monetize.md`（2026-07-15時点、上記の値下げ以外は現在も有効）

---

## 7. 事業承継（設計のみ、UI未実装）

日本ピアノ調律師協会の会員数は10年で約3,000人→約2,000人（年3〜4%減）。この自然減を「解約」ではなく「引き継ぎ」に変える設計。
- お客様の引き渡し（全部／エリア単位／個別）。DBに`handover_from`/`handover_at`カラムを設計済み
- お客様へのご案内は**前任者の名前で**届き、後任者の訪問者カード（顔写真・資格）を添える（お客様が離れる最大の理由「知らない人になった」への対策）
- アカウントごとの引き継ぎ（家族・弟子）、段階的な引き継ぎ（共同担当期間）、課金の扱い（初年度無料など）も設計案あり
- 売り文句：「あなたが引退するとき、お客様を守れます」（買い切りソフト競合にはこの発想自体がない）

---

## 8. 個人情報・プライバシー（未実装。要着手）

300人ぶんの氏名・住所・電話・メール、お客様宅の写真を扱う＝個人情報取扱事業者としての安全管理措置義務・漏洩時の通知/報告義務が発生。方針4つ：
1. **持たないものを決める**：パスワード不保持（Magic Link/OTP）、カード情報不保持（ストア課金/Stripeに一任）、写真は必要分のみ・保存期間を決めて自動削除（例：3年）
2. **同意の導線**：お客様ごとに「写真の保存：同意／辞退」を持つ。辞退なら端末内のみ保存
3. **消せるようにする**：お客様詳細から写真含め完全削除できる導線。削除した事実だけ記録に残す（氏名は伏せる）
4. **名乗る**：プライバシーポリシー・利用規約をアプリ内に、問い合わせ窓口を明示、漏洩時の対応手順を事前に決める

**GDPRを見据えた設計を推奨**（EU展開の二度手間防止）：保存先リージョンを選べる構造、削除要求の処理フロー、データ取り扱い記録の3点。

---

## 9. 技術仕様

> **2026-08-27に方針転換。詳細は `choritsu-note-arch.html`（実装アーキテクチャ 第3版）が最新の正。**
> 以下の旧案（Capacitor + Supabase）は**採用しない**。`choritsu-note-tech.html` は経緯として残すが、技術選定については古い。

### 採用する構成（2026-08-27決定）
```
Expo SDK 57（React Native 0.86 / TypeScript）
        ↓ EAS Build（Windowsから。Mac不要）
iOS / App Store   ＋   Android / Google Play
        ↓
Firebase（Firestore + Auth + Storage + Functions + Scheduler）
        ↓
RevenueCat（課金）／ FCM・APNs（通知）／ Firebase Hosting（お客様用ページ・別実装）
```

### なぜ変えたか（3点）
1. **Macは不要だった。** 既存アプリ「割ペイ」はExpo + EAS Buildで構成され、このWindows機からApp Storeに公開済み。旧仕様書が「最優先タスク」としたMac手配は、前提そのものが誤りだった
2. **Capacitor → Expo。** モックをそのまま包める利点より、既に回っている道具立て（EAS・RevenueCat・Firebase・両ストアのアカウント）を流用する利点が大きい。代償として9画面のUIはRNで作り直しになる
3. **Supabase → Firebase。** データは「1人の調律師が持つ木」で他人と交差せず、規模も小さい。運用中のバックエンドを増やす理由がない

### 割ペイと1点だけ変える：Firebase SDK
**@react-native-firebase（ネイティブSDK）を使う。** 割ペイが使うFirebase JS SDKは、React NativeではFirestoreのオフライン永続化が効かない（IndexedDB不在のため。検証済み）。調律師は電波の弱い顧客宅で作業するので、オフラインで開けないのは致命傷。

### 旧案（不採用、記録として）
- ~~TypeScript + Svelte 5 + Vite → Capacitor iOS/Android + PWA~~
- ~~Supabase（Postgres + RLS）~~
- 認証はパスワードを持たない方針（Magic Link / OTP）は**継続**

### DBスキーマの要点（詳細は `choritsu-note-tech.html` §4）
- テーブル：`tuners`（調律師）／`customers`／`pianos`／`visits`＋`visit_pianos`／`records`＋`record_works`
- **最重要の制約**：`records.kind = 'tuning'` のときだけ `pianos.last_tuned_on` を更新する。DBトリガーで守るのが安全
- 住所カラムは `addr_country/addr_postal/addr_region/addr_city/addr_line1/addr_line2` の国際標準形

### オフライン方針
深追いしない。当日の訪問先・前回記録をIndexedDBに保持、写真とメモは端末に貯めて後から送信、で打ち止め。

### ストア展開・リリース日程（2026-08-27更新）
- **両ストアの開発者アカウントは既に保有**（割ペイで登録済み）。新規登録は不要
- **iOSビルドにMacは不要**（EAS Buildがクラウドの macOS でビルドする）。旧記述「Mac必須・手配が最優先」は撤回
- **Google Playの「12人×14日」はアプリ単位**（アカウント単位ではない）。割ペイで通しても調律ノートは免除されない。しかも割ペイ自身がまだPlay未公開（クローズドテスト中）
- → **最小ビルドを最初にPlayのクローズドテストへ投入し、14日カウントを回しながら中身を作る**。これは旧仕様書の判断がそのまま生きる
- Small Business Program（Apple、手数料15%）は自己申請制。割ペイで課金しているので通っている可能性が高いが**要確認**

### 直近の最優先タスク
1. **フェーズ0**：Expo SDK 57で新規作成、Firebaseプロジェクト新規作成、EAS設定、両ストアにアプリ登録
2. **起動して1画面出るだけの最小ビルドをPlayクローズドテストへ投入**（14日カウントを最初に回す）
3. テスター12人以上の確保（1人抜けるとカウントがリセット）
4. react-native-firebase 26 と RN 0.86 / New Architecture の実地の相性を、この最小ビルドで最初に確認する

---

## 10. デザイン/実装上の注意（ハマりどころ）

- 日付は必ずローカル日付で扱う（`toISOString`はUTCで1日ズレる → 自前の`iso()`関数を使用済み）
- localStorageキーはバージョニングされている。データ構造を変えたらバージョンを上げる
- PWAのService WorkerはHTTPS（またはlocalhost）でのみ動く。ファイル直開き(file://)では登録されない
- `records.kind` と `pianos.last_tuned_on` の関係が設計上いちばんの勘所（§9参照）

---

## 11. 次のアクション（優先順、2026-08-27更新）

1. **【最優先・未実施】実際の調律師にモックを見せて検証**：「年14,800円払うか」の確認。手順・トークスクリプト・インタビュー質問・記録シートは `検証準備.md` に用意済み。あとは実施するだけ
2. `deploy/`フォルダをどこかにホスティングし、その場でスマホから開けるURLを用意する（検証の実施を後押しする。Vercel等、未着手）
3. テスター募集の案内文の作成（Google Playクローズドテスト用。①の検証がYesと出てから着手でよい）
4. 最小ビルド（クローズドテスト用）に含める機能範囲の決定
5. 開発環境の整備（Capacitorセットアップ、Mac/クラウドビルドの選定）
6. プライバシーポリシー・利用規約の作成（未着手）
7. 事業承継機能のUI実装（DB設計のみ済み、画面は未実装）

---

## 12. 同梱ファイル

| ファイル | 内容 |
|---|---|
| `HANDOFF.md` | この引き継ぎ書 |
| `choritsu-note.html` | **アプリ本体（最新モック、全18機能実装済み）**。実装時の仕様書として使う |
| `choritsu-note-arch.html` | **実装アーキテクチャ 第3版（2026-08-27）＝技術選定の最新の正**。Expo+Firebase、作る順番 |
| `choritsu-note-tech.html` | 技術仕様書（2026-08-10）。**技術選定は古い**（Capacitor+Supabase）。DB項目の定義と業務ルールは今も有効 |
| `choritsu-note-spec2.html` | 単発案件・個人情報・事業承継・フリーミアム・国際化の検討資料 |
| `choritsu-note-monetize.md` | 初期のマネタイズ調査・競合価格・顧客獲得チャネル（§6の値下げ以外は現在も有効） |
| `検証準備.md` | 調律師への検証（デモの見せ方・価格の伝え方・インタビュー質問・記録シート） |
| `deploy/` | PWA公開用一式。**新しいchoritsu-note.htmlの内容で2026-08-27に再ビルド済み**（キャッシュはv2） |
| `choritsu-note-app.zip` | 再ビルド後のdeployフォルダのZIP（配布用） |
| `iphone-install-guide.md` | iPhoneへのインストール手順 |
| `make_icon.py` | アイコン再生成スクリプト（Pillow使用。アイコン自体はデザイン変更なしのため未実行） |
| `serve.ps1` | ローカル確認用の簡易サーバー（`http://localhost:8934/`。ルートは`choritsu-note.html`、`/deploy/`配下はPWA版） |

※ 機能改修は `choritsu-note.html` を直す。PWA公開時は `deploy/index.html` に反映し直すか、`make_icon.py`のあるフォルダで再度PWA化する。

---

## 13. iOSビルドとTestFlight（2026-08-29 実施）

**TestFlightへの提出まで完了。** 0.1.0 (build 5)。中身はフェーズ0の足場で、
移植した業務ロジックの動作確認用の1画面。9画面の実装はこれから。

| | |
|---|---|
| Bundle ID | `jp.izumikawa.choritsunote` |
| App Store Connect | 調律ノート / Apple ID `6806505606` |
| Apple Team | `KPXFL5CQN8` |
| EASプロジェクト | `@manta55/choritsu-note` |
| ASC APIキー | `5F4VNLGQ85`（Expo に登録済み） |
| 配布証明書 | 有効期限 2027-08-29 |
| プロビジョニング | `choritsu-note AppStore` |

### Windowsだけで署名を通した手順

Macは使っていない。EAS CLIはTTYを要求するため対話操作ができず、
ExpoのWeb UIも証明書の「アップロード」専用で生成機能がない。そのため手作業で作った。

1. `openssl genrsa` と `openssl req` で秘密鍵とCSRを生成
2. Developer Portal にCSRを提出 → Apple Distribution 証明書を発行・ダウンロード
3. `openssl pkcs12 -export -legacy` で `.p12` を作成
4. Developer Portal でApp Store用プロビジョニングプロファイルを生成
5. Expoの資格情報ウィザード（8ステップ）に `.p12` とプロファイルを登録

### 詰まった点（次回のため）

**`.p12` は必ず `-legacy` を付けて作る。** OpenSSL 3 の既定
（PBES2 / PBKDF2 / AES-256-CBC）はビルド機のmacOSキーチェーンが取り込めず、
`Distribution certificate ... hasn't been imported successfully` で失敗する。
`-legacy` を付けると `pbeWithSHA1And40BitRC2-CBC` になり通る。

**RN 0.86.0 はHermesの既知不具合があり 0.86.3 以降にする。**
expo-doctor が検出する。割ペイも 0.86.0 なので同じ確認をした方がよい。

**Appleのページで Google 翻訳が動いていると表示が化ける。**
バンドルIDが `jp.izukawa` に見えるなど。値はDOMから直接読んで確認すること。

### 次にやること

1. 9画面の実装（フェーズ1）。試作 `choritsu-note.html` が仕様書になる
2. Firebaseプロジェクトの作成と接続
3. TestFlightの内部テスターに自分を追加して実機確認

---

## 14. TestFlight 内部テスト（2026-08-29）

内部グループ **Internal**（自動配信オン）を作成し、`manabu@izumikawa.jp` を追加。
0.1.0 の build 5 / 6 が両方ひもづき、招待が出る状態になっている。
外部テストはまだ使っていない（内部テスターは審査なしで即配信できるため）。

## 15. フェーズ2：ご案内の送信導線（2026-08-29）

**送信サーバは持たない。** 端末の LINE / SMS / メールへ本文を渡して開くだけにしている。
理由は、お客様の連絡先をこちらの外に出さないこと、調律師がふだん使っている
宛先と履歴がそのまま使えること、届かなかったときの確認がいつものアプリで完結すること。

| ファイル | 役目 |
|---|---|
| `src/lib/messages.ts` | 文面（調律のご案内／訪問前のご案内）と予約ページURL |
| `src/lib/channels.ts` | 送信先URLの組み立て。react-native を読み込まないので検算できる |
| `src/lib/send.ts` | 実際に開く。`Linking` と `Platform` はここだけ |
| `src/components/SendSheet.tsx` | 送る前に本文を必ず見せるシート。コピー導線つき |
| `src/components/ReminderSheet.tsx` | 調律のご案内。開いた時点で予約ページの鍵を作って保存する |

### 決めたこと

**LINE は宛先を指定して開けない。** 公開されている方法が無いため
`https://line.me/R/share?text=` で共有画面を開き、お客様はそこで選んでいただく。
シートにもその旨を書いてある。仕様の限界であって、実装の手抜きではない。

**SMS の本文の付け方は端末で違う。** iOS は `&body=`、Android は `?body=`。
`channels.ts` は区切り文字を引数で受け、`send.ts` が `Platform.OS` で渡す。

### フェーズ1の移植で落ちていたもの（この回で直した）

**見送った台が一覧から消えていた。** `pendingPianos` が見送り済みの台を除いていたため、
「見送る」を押し間違えるとお客様ごと画面から消え、取り消す手立てが無くなっていた。
試作（`choritsu-note.html`）は見送った台を残して「取り消す」を出している。同じ形に戻した。
除外するのは見出しの台数と、予定画面の「時期を迎える（日付未定）」だけ。

**ダークテーマで主ボタンの文字が 2.22:1 だった。** `Button` が文字色を `#FFFFFF`
決め打ちにしていた。パレットには地ごとの文字色（`onAccent`）があるのに使っていない。
`onOverdue` を足し、選択中のチップ7箇所の `#FFF` も含めて全部パレット参照に変えた。

**押せないボタンを薄くすると読めなくなる。** `opacity: 0.45` は地と文字を一緒に沈めるため、
文字と地のコントラストまで落ちる（送信ボタンで 2.32:1、未登録のLINEで 3.83:1）。
薄くするのをやめ、`surface2` の地に `ink3` の文字を置いて「いまは押せない」を示す形にした。

### 確認したこと

- `npx tsx app/src/lib/__check.ts` … 35件すべて通過（フェーズ2で14件追加）
- Webへ書き出して実描画で確認：ご案内送信・見送り・取り消し・訪問前のご案内
- コントラスト：ライト／ダークの全5タブとシート2種で、実際に塗られている地に対して
  AA（4.5:1、大きい文字3:1）を満たさない文字は0件
- タップ領域：新しく足した操作はすべて44px以上

### まだ無いもの

`/b/{token}` のお客様用ページは**まだ公開していない**。文面にURLは入るが、
リンク先が無い。ページはお客様の応答（お願いします／見送ります）を書き戻す先が要るため、
Firebase の用意が前提になる。ドメイン `choritsu-note.app` も未取得。
置き場所を変えるときは `src/lib/messages.ts` の `BOOKING_ORIGIN` だけ直せばよい。

## 16. Firebase（2026-08-29 用意まで）

| | |
|---|---|
| プロジェクト | `choritsu-note`（割ペイの `warikan-bu` とは分ける） |
| 親リソース | `izumikawa.jp`（Workspace組織配下のため選択が必須だった） |
| Google アナリティクス | **無効**。お客様の氏名・住所を扱うので既定では入れない。必要になれば後から有効化できる |
| Firestore | Standard エディション / `(default)` / **asia-northeast1（東京）** |
| セキュリティルール | 本番環境モード（`allow read, write: if false`）で作成。まだ何も開けていない |

ロケーションは**後から変更できない**。日本のお客様の個人情報を置くので東京にした。

まだ**アプリの登録（iOS / Android）はしていない**。登録すると
`GoogleService-Info.plist` と `google-services.json` のダウンロードが要る。
`@react-native-firebase` の導入とデータ層の書き換えは、Firestore の
コレクション設計とセキュリティルールを決めてから一度にやる。

## 17. 0.2.0 の配信（2026-08-29）

`0.2.0 (build 7)` を EAS でビルドし、TestFlight へ提出済み。
Internal グループは自動配信なので、Apple の処理が終われば内部テスターに届く。
中身はフェーズ2まで（保存はまだ端末内の AsyncStorage）。

## 18. Firestore の設計とセキュリティルール（2026-08-29）

設計の考え方は `firestore-design.md`。実物は `firestore.rules` / `storage.rules`。

要点だけ：

- **お客様は台帳を読まない。** お客様に見せてよい項目だけを写した
  `bookings/{token}` を別に置き、そこだけを開ける。写しに住所・電話・メモ・
  写真・売上は入れない。鍵が漏れても、漏れるのはその写し1件だけ
- **`allow list: if false`。** 1件取得だけに絞る。鍵を長くしても、
  コレクションを一覧できれば意味がない
- **お客様が書けるのは `reply` / `want` / `intake` / `repliedAt` の4つだけ。**
  1つでも他に触れた書き込みは、まるごと拒否する
- **お客様1件＝1ドキュメント**（ピアノと履歴を中に持つ）。4台・30年ぶんでも
  84KB 程度で、1MiB の上限に対して1割弱。写真だけは Storage に出す

### 配ったあと、本物に対して確かめている

`node tools/rules-check.mjs` が、**本番の choritsu-note を実際に叩いて** 23件を確認する
（他人の台帳が読めないこと、一覧が引けないこと、お客様が料金や日時を書き換えられないこと、
鍵を作り直したら開かなくなること、など）。エミュレータを使っていないのは、
手元のファイルが正しくても配り忘れていれば意味がないため——割ペイで一度これに引っかかっている。

Javaが入っていないので Firestore エミュレータは動かせない。ルールのユニットテストを
足すなら JDK が要る。いまは上の実地確認で代えている。

### Firebase 側の設定（この回で入れたもの）

- ウェブアプリ `choritsu-note booking` を登録（App ID `1:256206159985:web:483f4f088cbabf83de5bd0`）
- **匿名認証を有効化**。お客様の予約ページが使う。姿は見えないがサインインさせることで、
  App Check を効かせられ、悪用時にたどれる手がかりも残る
- 匿名アカウントの**自動クリーンアップを有効化**（30日）。ページを開くたびに増えるため

ウェブAPIキーは秘密ではない（プロジェクトを指すだけで、何も許可しない）。守っているのはルール。

## 19. クラウド接続とお客様用ページ（2026-08-29）

### 台帳の正は、端末の中のまま

`@react-native-firebase` を入れたが、**AsyncStorage を置き換えてはいない**。
端末の中が正で、クラウドへは控えを送る。サインインしていなくても、圏外でも、
クラウドが落ちていても、アプリは今までどおり動く。

そうした理由は2つ。ひとつは、このアプリでいちばん取り返しがつかないのが
「端末を無くして台帳ごと消える」ことで、それを防ぐには控えがあれば足りること。
もうひとつは、ネイティブモジュールはブラウザでは確認できず、実機ビルドを
1回使うまで検証できないので、壊れたときの影響範囲を小さく保ちたかったこと。

| ファイル | 役目 |
|---|---|
| `src/store/cloudTypes.ts` | クラウド入口の形（型だけ） |
| `src/store/cloud.native.ts` | 実機の実装。Firebase を触るのはここだけ |
| `src/store/cloud.ts` | Web の確認環境用。何もしない実装 |

**Metro のプラットフォーム拡張子で分けている。** 実機は `.native.ts`、Web は `.ts` を読む。
おかげでブラウザでの画面確認は今までどおり続けられる。

### サインイン

**Apple でサインイン**（iOS）。パスワードを覚えなくてよいのが第一の理由。
氏名は求めない（本人を見分けるのはメールアドレス）。**Android はまだ**——
Google サインインは Android 着手時に足す。

サインインは任意。していなくても全機能が使える。設定画面に「いまは、この端末の
中だけに保存されています」と出して、黙って危ない状態にしない。

### 控えの向き

サインインした直後だけ、どちらを正にするか決める。

- 端末の中が**まだサンプルのまま**（一度も編集していない）→ クラウドを取り込む（機種変更）
- 一度でも触っていれば → 端末の中を送る

この判定が無いと、新しい端末のサンプル8軒でクラウドの本物の台帳を
上書きしてしまう。印は `choritsu.pristine.v1` に持ち、最初の書き込みで消える。

### お客様用ページ

`https://choritsu-note.web.app/b/{token}` で公開済み（Firebase Hosting）。
`web/b/index.html` の1枚。お客様はアプリを入れない。

- 匿名サインイン → `bookings/{token}` を1件だけ読む → 「お願いします」か「今回は見送ります」
- 「お願いします」のあとに、ご希望の時期・気になっていること・気づいた場面を任意で聞く
- **日付は選ばせない**。日程は調律師が決める（重複予約が起きない）

ご案内を送るときにアプリが写しを置き、送ったぶんの鍵を1件ずつ見張って、
お返事が来たら台帳の「ご依頼」または「お客様が見送り」に変える。
一覧では引かない（`allow list: if false` のまま）。

**本番で通しの確認ずみ**：ページを開く → お願いします → 問診を入れて送信 →
Firestore 側に `reply: 'yes'` と希望・問診が入っていることを確認 → 完了画面。
ライト・ダーク両方で、AA を満たさない文字は0件。

独自ドメインは未取得。`src/lib/messages.ts` の `BOOKING_ORIGIN` だけ直せば移せる。

### ビルド前に確かめたこと

Windows では iOS の prebuild が動かないので、**Android で prebuild を通して**
設定プラグインが効くことを確認した（`google-services.json` の配置、
`com.google.gms.google-services` の適用）。生成された `android/` は捨て、
`.gitignore` に入れてある。expo-doctor 21件通過。

`expo-system-ui` を足した（`userInterfaceStyle` の警告が出ていたため）。

### Firebase 側の設定（この回で入れたもの）

- iOS アプリ `1:256206159985:ios:72d4c267f0c67bccde5bd0`
- Android アプリ `1:256206159985:android:c8bd8c12fed76dd1de5bd0`
- 設定ファイルは `apps:sdkconfig --out` で書き出し、`app/` 直下に置いてリポジトリに入れた
  （ウェブAPIキーと同じで秘密ではない。守っているのはセキュリティルール）
- **Apple ログインを有効化**。iOS ネイティブの流れなのでサービスIDは不要
- Hosting を有効化。`/b/**` は `noindex` と `no-store` を付けている

### 0.3.0 のビルドで詰まった3点（次回のため）

**1. pod install が落ちる（SPM と静的リンクの衝突）**

react-native-firebase 26 は Firebase を SPM で解決する。SPM は静的リンクと
両立しない（各 pod が Firebase の写しを抱え、リンク時に重複シンボルになる）。

対処は `app.json` のプラグイン設定で CocoaPods 側に寄せる。

```json
["@react-native-firebase/app", { "ios": { "disableSPM": true } }]
```

`useFrameworks: "static"` はそのまま。動的リンクにすると起動が遅くなるうえ、
Xcode 26 の SPM は追加の設定が要るとパッケージ側が書いているため。

**2. Provisioning Profile に Sign In with Apple が入っていない**

`expo-apple-authentication` を足しても、Apple Developer 側の App ID で
capability を有効にしないと署名で落ちる。

Developer Portal → Identifiers → `jp.izumikawa.choritsunote` →
チェックボックス `APPLE_ID_AUTH` を入れて保存 →
**「アプリの機能を変更する」ダイアログで「確認する」を押す**（押さないと静かに未保存）。
翻訳で表示が化けるので、値は DOM の id で確かめること。

**3. capability を変えると、既存のプロファイルが無効になる**

Apple 側で無効化されるが、Expo に保存された記録は "Valid" のままなので、
そのままビルドすると同じ失敗を繰り返す。Expo の認証情報ページで
**プロビジョニングプロファイルだけを削除**し（証明書は残す）、EAS に作り直させる。

ただし非対話モードでは Apple Team ID を聞かれて止まる。環境変数で渡す：

```bash
EXPO_APPLE_TEAM_ID=KPXFL5CQN8 EXPO_APPLE_TEAM_TYPE=INDIVIDUAL npx eas-cli build --platform ios --profile production --non-interactive
```

これで EAS が capability を同期した新しいプロファイルを自動で作る。
`.p8` の鍵ファイルを手元に置く必要はない。

## 20. 残作業の棚卸し（2026-08-30）

### やったこと

**Android の Google サインイン（コードのみ）**

サインインの入口を `signInMethods` / `signIn(method)` に一般化した。
iOS は Apple、Android は Google を返す。画面はどちらが来ても同じ形で出す。

Firebase 側で **Google プロバイダを有効化**（公開名「調律ノート」、
サポートメール manabu@izumikawa.jp）。ウェブ クライアント ID は
`256206159985-ah138ob5ah1nj0ltaaa9uupttiltmaih.apps.googleusercontent.com`
（`google-services.json` の client_type: 3。秘密ではない）。

**まだ動かない。** Android の Google サインインは、実行時に
**SHA-1 の登録**が要る。しかも Play 経由で配ると Google が署名し直すので、
本番で効くのは **Play App Signing の証明書の SHA-1**。これは Play Console に
アプリを登録しないと出てこない。つまり順番は
「Play Console 登録 → App Signing の SHA-1 を Firebase に登録 →
`google-services.json` を取り直す → Android ビルド」。

そのため **Android ビルドはまだ流していない**。SHA-1 が入る前に建てても
サインインが失敗するだけで、ビルドを1本無駄にする。

**Play Console のアプリ作成フォームは途中まで入れてある**
（調律ノート / jp.izumikawa.choritsunote / 日本語 ja-JP / アプリ / 無料）。
残っているのは「デベロッパー プログラム ポリシー」と「米国輸出法」の
2つの申告チェックだけ。これは法的な申告なのでオーナー本人が入れる。

### できなかったこと（判断がオーナー側にある）

**Cloud Storage は Blaze プラン（有料）が必要だった。**
無料枠で選べるロケーションは US-EAST1 のみ。Firestore を東京に置いた以上、
お宅の中が写る写真を米国に置くのは筋が通らないので ASIA-NORTHEAST1 を
選んだところ、料金プランのアップグレードを求められた。課金は
オーナーの判断なので、ここで止めてある。`storage.rules` は書いてあり、
バケットができたら `firebase deploy --only storage` で配れる。

**写真の自動削除（Cloud Functions）は、そもそも時期が早い。**
写真は型（`photoBefore` / `photoAfter`）があるだけで、撮影も保存も
まだ実装していない。消す対象が無いものを消す仕組みから作らない。
Blaze が要るのも同じ理由で保留。

## 21. 写真機能と Android の下ごしらえ（2026-08-30）

### 写真

`photoBefore` / `photoAfter` は型だけあって未実装だったので、実装した。

**画像そのものは台帳に入れない。** 端末のファイルとして持ち、記録には
**ファイル名だけ**を入れる。理由は2つ。Firestore は1件1MiBまでで写真を入れると
一発で当たること。もう1つは、台帳を1軒開くたびに写真まで運ぶことになること。

| ファイル | 役目 |
|---|---|
| `src/lib/photos.ts` | 撮る・縮める・保存・消す。expo-file-system の新API（`Paths`/`File`/`Directory`） |
| `src/components/PhotoField.tsx` | 作業前・作業後の1枠ぶん |

- 長辺 **1280px** に縮めて JPEG（品質0.72）で保存する
- **お客様が写真を辞退されている場合は、そもそも取り込ませない**。運用ではなく
  `PhotoField` が止める（`consent === 'no'` で入力欄ごと出さない）
- 記録・ピアノ・お客様を消したら、ひもづく写真も端末から消す（`dropPhotos`）
- 権限を断られたときは例外にせず、戻り値で理由を返して画面に出す

**写真はクラウドへ送らない。** Cloud Storage が Blaze プラン（有料）を要求するため。
新しい端末で台帳を戻しても写真は来ないので、その旨を画面に出している
（設定の控え欄と、ファイルが無い写真枠）。

iOS のカメラ・写真の権限文言は `app.json` の `expo-image-picker` に入れた。

### Android

- **Play Console にアプリを作成**（`4976287948192301459`。調律ノート /
  jp.izumikawa.choritsunote / 日本語 / アプリ / 無料）
- **SHA-1 を2つ Firebase に登録**した
  - Play App Signing: `B8:D0:EA:C8:AD:9A:89:4A:26:FE:73:ED:13:17:84:21:DF:E7:39:8C`
  - EAS の upload key: `10:A6:92:61:89:14:70:07:DD:9D:71:69:85:A7:53:AE:10:02:61:99`
- `google-services.json` を取り直し、Android の OAuth クライアント（client_type 1）が
  2件入っていることを確認した

Play 経由で配ると Google が署名し直すので、**本番で効くのは Play App Signing のほう**。
EAS が直接配る APK では upload key のほうが効く。両方入れてあるのはそのため。

### コンソールで値を取り出すときの小技

Play Console も Expo も、SHA-1 を「コピー」ボタンでしか出さない（DOMには無い）。
`navigator.clipboard.readText()` は権限の確認で固まることがあるので、
**ページに入力欄を差し込んで Ctrl+V で貼る**のが確実だった。

## 22. Play Console の申告（2026-08-30 完了）

### データセーフティは CSV で入れる

画面をひとつずつ押していく方式は、Angular が合成クリックを受け取らないうえに
ページが重く、途中でレンダラが固まる。**「CSV からインポート」を使う。**

1. データセーフティ画面の右上「CSV にエクスポート」で今の回答を落とす
   （落ちてくるのは *保存済み* の回答だけ。画面上の未保存の入力は入らない）
2. `Response value` 列に `true` / `false` / URL を書く。空欄＝未チェック
3. 「CSV からインポート」→ ファイルを渡す → インポート
4. ステップ2〜4がすべて埋まった状態になるので、5のプレビューを見て「保存」

`store/data-safety.csv` が実際に読ませたファイル。782行あるが、値が入っているのは38行。
質問IDは `PSL_DATA_USAGE_RESPONSES:<データ種別>:<設問>` の形。
実装を変えたら、このファイルと `store/listing.md` の表を直してから読ませ直す。

アップロード欄の `input[type=file]` は DOM にあるが `hidden`。
`i.style.display='block'` などで見えるようにすれば、そこへファイルを渡せる。

### 申告の中身

- 収集：名前／メールアドレス／ユーザーID／住所／電話番号／その他のユーザー作成コンテンツ
- 共有：**なし**（ひとつも無い）
- 一時的な処理：すべて「いいえ」
- 必須か：すべて「ユーザーが選択できる」（サインインしないと端末から出ないため）
- 用途：アプリの機能。メールアドレスとユーザーIDのみ「アカウント管理」も付く
- 写真は申告していない。Cloud Storage を使っておらず端末から出ないため

「アプリのコンテンツ」は全項目が [対応済み] に入った。要注意は空。

### ストア掲載文の料金の記述を直した

有料プランは **課金の実装が無い**（`SettingsScreen` で選ぶだけで、支払いは発生しない）。
価格を書いたままにすると、コンテンツレーティングの「デジタル購入＝いいえ」と食い違う。
掲載文を「無料でお使いいただけます。アプリ内での課金は現在ありません。」に差し替え、
`store/listing.md` のレーティング表も「デジタル購入＝なし」に直した。

課金を実装したら、**掲載文・レーティング・`store/listing.md` の3か所**を同時に戻すこと。

### まだ手でやってもらう必要があるもの

- **ストア画像7枚のアップロード**。「アセットを追加」はネイティブのファイル選択を開くため、
  こちらからは渡せない。`store/icon-512.png`、`store/feature-1024x500.png`、
  `store/screenshots/*.png` の5枚を、アイコン／フィーチャー／スマートフォン／
  7インチ／10インチの各欄へ。画像が入るまで掲載情報は「保存」できず、
  「未公開として保存」（下書き）までしか進まない
- Play への提出（`eas submit --platform android` はサービスアカウント鍵が要る）
- Blaze の請求先（Cloud Storage と写真の自動削除がこれ待ち）

## 23. モック（choritsu-note.html）とRN実装の全画面突き合わせ（2026-09-05）

10画面（ご案内・予定・お客様・詳細・追加・ピアノ追加・記録・売上・売上帳・設定）を
モックのHTML/JSと1つずつ突き合わせた。結果と対応方針は以下のとおり。

### 直したもの（このセッションで実装済み）
- 予定画面：リスト/カレンダーの切り替えが無かった → カレンダー表示（月グリッド、日付選択、凡例）を追加
- 予定の各行に「地図」ボタンが無かった → 追加
- **お客様・ピアノの情報を登録後に一切編集できなかった**（最重要）→
  `EditCustomerScreen` / `EditPianoScreen` を新設し、`AppContext` に `updatePiano` を追加。
  `CustomerDetailScreen` に「お客様の情報を直す」「（ピアノごとに）直す」ボタンを設置

### 見つかったが未対応（優先度順、次回の参考に）
- 1台目のピアノ（`AddCustomerScreen`経由）は調律周期が12ヶ月固定。2台目以降（`AddPianoScreen`）は変更可能で不整合
- `RecordFormScreen`に「ピアノの状態（問題なし／気になる点）」の入力欄が無い。
  `WorkRecord.cond` は型として存在し保持もされるが、入力するUIが無い
- `RecordFormScreen`に「前回と同じ内容にする」ボタンが無い（モック・HANDOFF §4に明記の現場軽量化機能）
- 「追加で必要な作業をその場で承諾してもらう」機能（B層、HANDOFF §3行65明記）が丸ごと未実装
- `CustomersScreen`に「エリア」表示（4エリアの簡易マップ、`areaBucket`相当）が無い。一覧のみ
- `HomeScreen`の「今日の訪問」から地図・電話・記録開始をワンタップでできない（モックにはあった）
- `HomeScreen`に「今月、ご案内から生まれた売上」（ROIバナー）の表示が無い
- お客様からの依頼カードに「取り下げ」ボタンが無い（「日程を決める」はある）
- 大量顧客時の「区分ごとに5軒で打ち切り『もっと見る』」（HANDOFF §4に明記）が未実装。全件表示のまま

### HANDOFF記載とコードの矛盾（要確認）
- §3「実装済み機能」に「お客様詳細に『臨時のご依頼を受ける』画面」と明記されているが、
  該当する画面・関数がコード上に見当たらない。実装漏れの可能性が高い

### 意図的にスコープ外と判断できたもの（対応不要）
- 前日確定アシスタント／繁閑の平準化 → §6で「有料機能に寄せる」と明記された先送り
- 会計CSVエクスポート → `LedgerScreen.tsx`冒頭のコメントに「アプリ内で完結させる」設計判断が明記
- 見積もり送信／作業報告書の送信（共有）／領収書発行 → §3の18機能リストに含まれず、モックのみの機能

## 24. §23の未対応項目をすべて実装（2026-09-05）

§23で「見つかったが未対応」としていた項目を、HANDOFF記載の矛盾（臨時のご依頼）も含めてすべて実装した。

- **`AddCustomerScreen`**: 1台目のピアノの調律周期を選択可能に（1年／半年／2年）。2台目以降との不整合を解消
- **`HomeScreen`**:
  - 「今日の訪問」各行に電話・地図・記録開始ボタンを追加（1台のみなら`RecordForm`へ直接、複数台なら`CustomerDetail`へ）
  - 「今月、ご案内から生まれた売上」バナーを追加（タップで売上タブへ）
  - 依頼カードに「取り下げ」ボタンを追加
  - 区分ごとに5軒で打ち切り「もっと見る」を`Section`コンポーネントに実装（`CAP = 5`）
- **`CustomersScreen`**: 「一覧」/「エリア」の切り替えを追加。`lib/select.ts`に`areaBucket`を新設（横浜市／川崎市／東京都／その他）。
  モックの地図的なピン配置は、件数バッジ付きのボタン一覧に簡略化（本質機能＝エリア絞り込みは同一）
- **`RecordFormScreen`**:
  - 「前回と同じ内容にする」ボタンを追加（種別・作業内容・ピッチ・湿度・メモ・支払い方法・状態をコピー）
  - 「ピアノの状態」入力を追加（今回は問題なし／気になる点があった → 4項目×3段階）。
    前回「要相談」だった項目は今回「経過観察」から始まる（`condFromPrev`）
  - 「追加で必要な作業」の承諾機能を追加。`types/index.ts`に`WORK_MENU`（8種類）と
    `WorkRecord.addWorks` / `addWorksApprovedAt`を新設。選択→合計金額表示→
    モーダルで理由とリスクを提示→「承諾をいただいた」で日時を記録
  - `lib/select.ts`の`recordTotal`に`addWorks`ぶんを合算するよう修正（売上集計に自動反映）
- **`CustomerDetailScreen`**: ピアノカードに「次回の申し送り」表示を追加
  （`lib/select.ts`の`condTodo`。前回「要相談」だった項目を次回訪問時に警告表示）
- **`SpotJobScreen`（新設）**: 「臨時のご依頼を受ける」画面。ご依頼の種別（定期調律以外）・対象ピアノ・
  お困りごと（チェック5項目＋自由記述）・伺う日を入力し、`Visit`として追加。
  `Visit`型に`kind` / `intake`を追加

いずれもWeb版で実機相当の操作確認（選択・保存・再表示）まで実施済み。型チェックも通過。

## 25. ビルド前の再監査（2026-09-05）

§24の実装分を、モックのJSソースと1行ずつ突き合わせて再監査。2件の実バグと1件の未実装機能を発見・修正。

- **バグ：`RecordFormScreen`の「前回と同じ内容にする」が`kind`と`pay`まで引き継いでいた**。
  モックの実処理（`sameAsLast`相当）はwork/pitch/humid/memo/condのみをコピーする。
  `kind`まで引き継ぐと、定期調律の記録を誤って「修理」等に切り替えてしまい、
  `次回のご案内予定日`欄が消える（`kindDef.cycle && !existing`で表示制御しているため）などの
  実害があった。`setKind`/`setPay`を削除し、各項目にモック同様の truthy ガードを追加。
  ボタンラベルにも前回日付を追加（`前回（2026年3月）と同じ内容にする`）
- **未実装：前回「要相談」だった項目からの追加作業自動提案が抜けていた**。
  モックの`pickPiano()`は前回`cond`が`act`の項目から`addWorks`の初期値を自動提案する
  （action→整調、strings→弦交換、humid→防湿装置）。実装時に見落としていたため、
  `prev`の算出を`addWorks`の`useState`より前に移動し、遅延初期化関数で提案ロジックを追加
- **未実装：`RecordFormScreen`に「前回の記録」「事前のお申し出」の表示がなかった**
  （モックの`renderDiff`/`renderIntake`相当）。§24で`SpotJobScreen`から`Visit.intake`に
  お客様の事前のお困りごとを保存する導線は作ったが、記録画面側で一切表示していなかったため、
  せっかく聞き取った情報が調律師の目に触れないまま埋もれていた。
  - 「前回の記録」カード：前回日付・経過期間・作業内容・料金・ピッチ・湿度・申し送り・メモを表示
  - 「お客様からの事前のお申し出」カード：この台に対する最新の`intake`（`findIntake`で
    `customer.visits`から`pianoIds`一致かつ`intake`ありの最新1件を検索）を表示
  - いずれもWeb版で実データ（既存履歴・新規に登録した臨時のご依頼）を使って表示を確認済み

型チェック（`npx tsc --noEmit`）通過。この監査の後、TestFlight向けビルドを実施。

（このビルドは build 15。EASビルド→Apple提出まで完了し、TestFlightで配信中。）

## 26. App Store 審査提出の準備（2026-09-05）

TestFlightへの内部配信はできていたが、**App Store（審査提出）向けの準備は未着手だった**
（プライバシーポリシー・サポート導線・ストア掲載文・スクリーンショットのいずれも、
iOS向けとしては揃っていなかった）ため、審査に出せる状態まで整えた。

### 見つけて直したこと

- **`web/index.html`（トップページ）に、サポート連絡先とプライバシー関連ページへのリンクが無かった**。
  プライバシーポリシー（`/privacy/`）とアカウント削除ページ（`/delete-account/`）は
  2026-08-30時点で既に作成・公開されていたが、そこへの導線がトップページに無く、
  App Store Connect の「サポートURL」として出すには不十分だった。連絡先メールと
  両ページへのリンクを追加し、Firebase Hosting に再デプロイした
- **`app.json` の `ios.supportsTablet` が `true` のままだった**。UIはiPad向けに
  最適化されておらず（固定幅のフォーム、レスポンシブ対応なし）、有効のままだと
  App Store ConnectでiPad用スクリーンショットの提出も必須になる。実態に合わせて
  `false` に変更した（次のビルドから反映。iPadでのインストールはできなくなる）

### 新しく作ったもの

- **`store/listing-ios.md`**：App Store Connect提出に必要な項目の下書き一式
  （サブタイトル・キーワード・プロモーション文・年齢制限の回答・Appのプライバシー
  （データ種別ごとのAppleカテゴリへの対応表）・App Review向けメモ・輸出コンプライアンス）。
  説明文と非表示のデータ種別はGoogle Play向けの`store/listing.md`と揃えてある
- **`store/screenshots-ios/`**：iPhone用スクリーンショット5枚（1320×2868、6.9インチ表示）
- **`tools/shots-ios.mjs`** / **`tools/upscale-shots.ps1`**：iOS用スクリーンショットの生成スクリプト

### スクリーンショット撮影で踏んだ罠

この開発機（Windows + puppeteer-core経由のChrome）で、ビューポート幅を実機並みに
狭く（〜660px以下）してスクリーンショットを撮ると、下部タブの見出し「売上」の2文字**だけ**が
「赤ト」という無関係な字に化けて描画される再現性のある不具合に遭遇した。

切り分けた結果：
- DOM上の`textContent`は常に正しい（`売上` = U+58F2 U+4E0A）。React側のバグではない
- 同じ「売上」という文字列が、同じ画面の別の場所（太字でない箇所）では正しく描画される
- `font-family`を明示指定しても、`font-synthesis: none`を指定しても直らない
- `deviceScaleFactor`（1／2／3）を変えても直らない
- ビューポート幅だけが決定要因で、768px以上では発生しない

実機のiOSやXcodeのレンダリングパイプラインとはまったく別物のため、**この撮影専用の
Windows Chrome環境に固有の表示バグ**（フォントの異体字選択かヒンティングの不具合）と判断し、
アプリのコードは一切変更していない。撮影は768px幅で行い、あとから実寸
（1320×2868）へ拡大することで回避した（`tools/shots-ios.mjs`のコメント参照）。

### オーナー本人にしかできない残り

- App Store Connect へのログイン（Apple ID認証はこちらで代行できない）
- `store/listing-ios.md`の下書きをコンソールへ貼り付け／選択
- スクリーンショット5枚のアップロード
- 内容を確認したうえで「審査へ提出」を押すこと（元に戻せない操作のため）

## 27. 却下（マイク権限）への対応、アカウント削除、iOS課金の実装（2026-09-11〜12）

§26の状態で審査へ提出した（build 15）ところ、**Guideline 5.1.1（マイク権限の説明文がプレースホルダー）**
で却下された。原因調査の過程で、**Guideline 5.1.1(v)（アプリ内アカウント削除）**にも
違反していることが判明したため、あわせて対応した。さらにオーナーの意向で、これまで表示のみ
だった有料プランを、iOSに限り実際に課金できるよう実装した。

### 直したこと（マイク権限）

- `expo-image-picker`はカメラの動画撮影に備えて既定でマイク権限を要求するが、
  `app.json`にその文言を指定していなかったため、プラグインの既定文言
  "Allow $(PRODUCT_NAME) to access your microphone" がそのまま使われていた。
  このアプリは写真のみ（動画撮影機能なし）でマイクは不要なため、
  `microphonePermission: false` を指定し、権限自体を削除した

### 新しく実装したこと（アカウント削除、Guideline 5.1.1(v)）

これまでは「サインアウト」しかなく、アカウントそのものの削除は
`web/delete-account/`のページからメールで依頼→7日以内に対応、という運用だった。
Appleは「アプリ内で作成できるアカウントは、アプリ内で削除も開始できること」を求めており、
この運用は次の審査で高確率に指摘される状態だった。

- `cloudTypes.ts`に`deleteAccount()`を追加。`cloud.native.ts`で実装：
  Firestoreの`tuners/{uid}/customers/*`と`meta/settings`を削除したのち、
  Firebase Authのユーザー自体を削除する。セッションが古く`auth/requires-recent-login`に
  なった場合は、Sign in with Appleで再認証してからもう一度削除を試みる
  （このAppはApple以外のサインイン方法をiOSに提供していないため）
- 端末内の台帳（お客様データ）自体は削除しない。サインアウトと同じ扱いで、
  クラウド側のアカウントと控えだけを消す
- `SettingsScreen`の「台帳の控え」カードに「アカウントを削除する」ボタンを追加
  （確認ダイアログで取り返しがつかない旨を明記）

### 新しく実装したこと（iOS課金、RevenueCat）

`SettingsScreen`の有料プランボタンは、これまで`updateSettings({ plan: k })`を
呼ぶだけで実際の決済が発生しない表示専用のものだった。ストア掲載文の
「アプリ内購入なし」という説明と実装が食い違っており、審査官が試せば
すぐに気づく状態だったため、iOSに限り実際に購入できるよう実装した。

- **App Store Connect**：サブスクリプショングループ「調律ノート Pro」の下に、
  月額プラン（`jp.izumikawa.choritsunote.pro.monthly`、¥1,200）と
  年額プラン（`jp.izumikawa.choritsunote.pro.yearly`、¥12,000）を作成。
  価格・配信可否（175か国）・日本語のローカライズ済み表示名/説明を設定済み
- **RevenueCat**：新規プロジェクト「調律ノート」を作成し、割ペイで使っている
  In-App Purchase用APIキー（チーム共通）を流用してApp Store連携。
  上記2商品をエンタイトルメント「調律ノート_pro」に紐付け、
  デフォルトオファリングの`$rc_monthly`/`$rc_annual`パッケージに接続
- **クライアント**：`react-native-purchases`を追加。`store/cloud.ts`と同じ
  Web版（no-op）／実機版（`.native.ts`）の分け方で`store/purchases.ts`を新設。
  `AppContext`に`purchasePlan`/`restorePurchases`を追加し、
  `Purchases.addCustomerInfoUpdateListener`で有効なプランの実際の状態
  （更新失敗・解約・別端末での購入引き継ぎ）を`settings.plan`へ自動反映させる
  仕組みを追加した。サインイン/サインアウトに合わせて`Purchases.logIn`/`logOut`も呼ぶ
- `SettingsScreen`の「無料プランに戻す」（ローカルの状態を書き換えるだけの偽の解約）は
  削除し、有料時は「サブスクリプションを管理する」（iOSの購読管理画面を開く）に置き換えた。
  無料時のプランボタンは実際の購入処理を呼び、処理中の表示・失敗時のアラート・
  「購入を復元する」ボタンを追加した
- `store/listing-ios.md`の「App内課金」欄と「Appのプライバシー」表（購入履歴＝
  RevenueCat経由、第三者共有・トラッキングなし）を実態に合わせて更新した

### Androidは対象外（意図的なスコープ外）

`purchases.native.ts`は`Platform.OS === 'ios'`のときだけ課金を有効にする
（RevenueCat Android側の実キー・Google Playの商品設定が未着手のため）。
Android版は「無料プランに戻す」等の偽の解約ボタンごと削除されるため、
これまで表示だけできていた有料プランのボタンを押すと「この端末では購入できません」
という案内が出るだけになる。`store/listing.md`（Google Play向け）は
「デジタル購入なし」のままで実態と合っている（Android側は変えていない）。
Android向けに課金を実装する場合は、割ペイと同様にRevenueCat側でGoogle Play連携と
実キーの設定が別途必要。

### まだ手でやってもらう必要があるもの

- EAS Build（build 16）の実行。無料枠を1回消費するため、実行前に確認すること
- 実機（TestFlight）でのアカウント削除・購入・復元の動作確認
  （[[feedback_testflight_before_review]]の方針どおり、審査提出の前に必ず挟む）
- App Store Connectのサブスクリプション審査用スクリーンショット・審査メモの追加
  （実際の購入UIができたので、それを撮り直して差し替える）
- 内容を確認したうえで「審査へ提出」を押すこと（元に戻せない操作のため）
